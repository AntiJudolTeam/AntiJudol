# Runtime
ENVIRONMENT=development           # development | production
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=                        # debug | info | warn | error (defaults: debug in dev, info in prod)
KILL_SWITCH_PATH=.killswitch
